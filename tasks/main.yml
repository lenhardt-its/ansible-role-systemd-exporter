---
- name: gather operating system variables
  include_vars: "{{ item }}"
  with_first_found:
    - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
    - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
    - "{{ ansible_distribution_file_variety | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
    - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
    - "{{ ansible_distribution | lower }}.yml"
    - "{{ ansible_os_family | lower }}-{{ ansible_distribution_version.split('.')[0] }}.yml"
    - "{{ ansible_os_family | lower }}.yml"
  become: true
  tags: systemd_exporter

- name: create system group
  group:
    name: "{{ systemd_exporter_system_group }}"
    system: true
    state: present
  when: systemd_exporter_system_group != "root"
  become: true
  tags: systemd_exporter

- name: create system user
  user:
    name: "{{ systemd_exporter_system_user }}"
    system: true
    shell: "/sbin/nologin"
    group: "{{ systemd_exporter_system_group }}"
    groups: "{{ systemd_exporter_user_additional_groups }}"
    home: /
    createhome: false
  when: systemd_exporter_system_user != "root"
  become: true
  tags: systemd_exporter

- name: check if systemd_exporter is installed
  stat:
    path: "{{ systemd_exporter_binary_install_dir }}/systemd_exporter"
  register: __systemd_exporter_is_installed
  become: true
  tags: systemd_exporter

- name: gather currently installed systemd_exporter version (if any)
  command: "{{ systemd_exporter_binary_install_dir }}/systemd_exporter --version"
  args:
    warn: false
  changed_when: false
  register: __systemd_exporter_current_version_output
  when: __systemd_exporter_is_installed.stat.exists
  become: true
  tags: systemd_exporter

- block:
  - name: get latest release
    uri:
      url: "https://api.github.com/repos/povilasv/systemd_exporter/releases/latest"
      method: GET
      return_content: true
      status_code: 200
      body_format: json
      validate_certs: false
      user: "{{ lookup('env', 'GH_USER') | default(omit) }}"
      password: "{{ lookup('env', 'GH_TOKEN') | default(omit) }}"
    environment: "{{ proxy_env }}"
    no_log: "{{ not lookup('env', 'MOLECULE_DEBUG') | bool }}"
    register: _latest_release
    until: _latest_release.status == 200
    retries: 5

  - name: "set systemd_exporter version to {{ _latest_release.json.tag_name[1:] }}"
    set_fact:
      systemd_exporter_version: "{{ _latest_release.json.tag_name[1:] }}"
  when: systemd_exporter_version == "latest"
  delegate_to: localhost
  run_once: true
  become: true
  tags: systemd_exporter

- block:
  - name: get checksum list from github
    set_fact:
      _checksums: "{{ lookup('url', 'https://github.com/povilasv/systemd_exporter/releases/download/v' + systemd_exporter_version + '/sha256sums.txt', wantlist=True) | list }}"
    environment: "{{ proxy_env }}"
    run_once: true
    become: true
    tags: systemd_exporter

  - name: "get checksum for {{ go_arch }} architecture"
    set_fact:
      systemd_exporter_checksum: "{{ item.split(' ')[0] }}"
    with_items: "{{ _checksums }}"
    when: "('linux-' + go_arch + '.tar.gz') in item"
    become: true
    tags: systemd_exporter

  - name: download binary to local folder
    get_url:
      url: "https://github.com/povilasv/systemd_exporter/releases/download/v{{ systemd_exporter_version }}/systemd_exporter-{{ systemd_exporter_version }}.linux-{{ go_arch_map[ansible_architecture] | default(ansible_architecture) }}.tar.gz"
      dest: "/tmp"
      checksum: "sha256:{{ systemd_exporter_checksum }}"
    environment: "{{ proxy_env }}"
    register: _download_binary
    until: _download_binary is success
    retries: 5
    delay: 2
    delegate_to: localhost
    check_mode: false
    become: true
    tags: systemd_exporter

  - name: unpack binary
    unarchive:
      src: "/tmp/systemd_exporter-{{ systemd_exporter_version }}.linux-{{ go_arch_map[ansible_architecture] | default(ansible_architecture) }}.tar.gz"
      dest: "/tmp"
      creates: "/tmp/systemd_exporter-{{ systemd_exporter_version }}.linux-{{ go_arch_map[ansible_architecture] | default(ansible_architecture) }}/systemd_exporter"
    delegate_to: localhost
    check_mode: false
    become: true
    tags: systemd_exporter

  - name: propagate binaries
    copy:
      src: "/tmp/systemd_exporter-{{ systemd_exporter_version }}.linux-{{ go_arch_map[ansible_architecture] | default(ansible_architecture) }}/systemd_exporter"
      dest: "{{ systemd_exporter_binary_install_dir }}/systemd_exporter"
      mode: 0755
    notify: restart systemd_exporter
    become: true
    tags: systemd_exporter
  when: (not __systemd_exporter_is_installed.stat.exists) or (__systemd_exporter_current_version_output.stdout.split(" ")[2] != systemd_exporter_version)

- name: copy consul-agent service json
  template:
    src: consul_agent_service.json.j2
    dest: /etc/consul/systemd_exporter.json
    owner: consul
    group: consul
    mode: 0644
  when: systemd_exporter_create_consul_agent_service
  notify: reload consul
  become: true
  tags: systemd_exporter

- name: copy systemd service unit
  template:
    src: systemd_exporter.service.j2
    dest: /etc/systemd/system/systemd_exporter.service
    owner: root
    group: root
    mode: 0644
  notify: restart systemd_exporter
  become: true
  tags: systemd_exporter

- name: enable capabilities to ports lower than port 1024
  capabilities:
    path: "{{ systemd_exporter_binary_install_dir }}/systemd_exporter"
    capability: CAP_NET_BIND_SERVICE+ep
    state: present
  when: systemd_exporter_web_listen_port <= 1024
  tags: systemd_exporter

- name: copy systemd_exporter alert rules files
  copy:
    src: "{{ item }}"
    dest: "{{ systemd_exporter_config_dir }}/rules/{{ item }}"
    mode: 0644
    owner: "{{ systemd_exporter_system_user }}"
    group: "{{ systemd_exporter_system_group }}"
  with_items: "{{ systemd_exporter_alertrules_files }}"
  notify: reload systemd_exporter
  become: true
  tags: 
    - systemd_exporter
    - systemd_exporter_alertfiles

- name: create systemd service unit
  template:
    src: systemd_exporter.service.j2
    dest: /etc/systemd/system/systemd_exporter.service
    owner: root
    group: root
    mode: 0644
  notify: restart systemd_exporter
  become: true
  tags: systemd_exporter

- name: allow binding to port in selinux
  seport:
    ports: "{{ systemd_exporter_web_listen_port }}"
    proto: tcp
    setype: http_port_t
    state: present
  when:
    - ansible_version.full is version_compare('2.4', '>=')
    - ansible_selinux.status == "enabled"
  become: true
  tags: systemd_exporter

- name: check whether ufw status is active
  command: "ufw status"
  changed_when: False
  ignore_errors: True
  register: _ufw_status
  become: true
  when:
    - systemd_exporter_allow_firewall
    - ansible_os_family == 'Debian'
  tags: systemd_exporter

- name: check whether firewalld status is active
  command: "firewall-cmd --state"
  changed_when: False
  ignore_errors: True
  register: _firewalld_status
  become: true
  when: ansible_os_family == 'RedHat'
  tags: systemd_exporter

- name: configure ufw
  community.general.ufw:
    rule: allow
    port: "{{ systemd_exporter_web_listen_port }}"
    proto: tcp
    state: enabled
    comment: "Systemd Exporter"
  changed_when: False
  ignore_errors: True
  notify: reload ufw
  become: true
  tags: systemd_exporter
  when:
    - "'active' in _ufw_status.stdout"
    - ansible_os_family == 'Debian'
    - systemd_exporter_allow_firewall

- name: configure firewalld
  firewalld:
    port: "{{ systemd_exporter_web_listen_port }}/tcp"
    permanent: yes
    state: true
  changed_when: False
  ignore_errors: True
  when:
    - "'running' in _firewalld_status.stdout"
    - systemd_exporter_allow_firewall
    - ansible_os_family == 'RedHat'
  notify: reload firewalld
  become: true
  tags: systemd_exporter

- name: ensure service is started and enabled
  systemd:
    daemon_reload: true
    name: systemd_exporter
    state: started
    enabled: true
  become: true
  tags: systemd_exporter
